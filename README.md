
# Prefix-Autocomplete CLI program

A command-line utility written in C that reads a text file, builds a prefix trie from its contents, and gives you interactive autocomplete.
Prefix search in the terminal using the ncurses library.

### Build
> make trie

### Usage
> ./trie [path_to_text_file]
